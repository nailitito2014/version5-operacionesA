<form method="post" action="{{ path('via_recepcion_delete', {'id': via_recepcion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ via_recepcion.id) }}">
    <button class="btn">Delete</button>
</form>
