{% extends 'base.html.twig' %}

{% block title %}EstadoServicio{% endblock %}

{% block body %}
    <h1>EstadoServicio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estado_servicio.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ estado_servicio.nombre }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ estado_servicio.fecha ? estado_servicio.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FechaInsercion</th>
                <td>{{ estado_servicio.fechaInsercion ? estado_servicio.fechaInsercion|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estado_servicio_index') }}">back to list</a>

    <a href="{{ path('estado_servicio_edit', {'id': estado_servicio.id}) }}">edit</a>

    {{ include('estado_servicio/_delete_form.html.twig') }}
{% endblock %}
