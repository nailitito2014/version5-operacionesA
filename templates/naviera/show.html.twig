{% extends 'base.html.twig' %}

{% block title %}Naviera{% endblock %}

{% block body %}
    <h1>Naviera</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ naviera.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ naviera.nombre }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ naviera.fecha ? naviera.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FechaInsercion</th>
                <td>{{ naviera.fechaInsercion ? naviera.fechaInsercion|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('naviera_index') }}">back to list</a>

    <a href="{{ path('naviera_edit', {'id': naviera.id}) }}">edit</a>

    {{ include('naviera/_delete_form.html.twig') }}
{% endblock %}
