{% extends 'base.html.twig' %}

{% block title %}TipoCliente{% endblock %}

{% block body %}
    <h1>TipoCliente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_cliente.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo_cliente.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_cliente_index') }}">back to list</a>

    <a href="{{ path('tipo_cliente_edit', {'id': tipo_cliente.id}) }}">edit</a>

    {{ include('tipo_cliente/_delete_form.html.twig') }}
{% endblock %}
