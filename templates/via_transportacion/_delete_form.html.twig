<form method="post" action="{{ path('via_transportacion_delete', {'id': via_transportacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ via_transportacion.id) }}">
    <button class="btn">Delete</button>
</form>
