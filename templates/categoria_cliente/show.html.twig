{% extends 'base.html.twig' %}

{% block title %}CategoriaCliente{% endblock %}

{% block body %}
    <h1>CategoriaCliente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoria_cliente.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ categoria_cliente.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categoria_cliente_index') }}">back to list</a>

    <a href="{{ path('categoria_cliente_edit', {'id': categoria_cliente.id}) }}">edit</a>

    {{ include('categoria_cliente/_delete_form.html.twig') }}
{% endblock %}
