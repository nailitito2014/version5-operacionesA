{% extends 'base.html.twig' %}

{% block title %}TipoMoneda{% endblock %}

{% block body %}
    <h1>TipoMoneda</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_moneda.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo_moneda.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_moneda_index') }}">back to list</a>

    <a href="{{ path('tipo_moneda_edit', {'id': tipo_moneda.id}) }}">edit</a>

    {{ include('tipo_moneda/_delete_form.html.twig') }}
{% endblock %}
