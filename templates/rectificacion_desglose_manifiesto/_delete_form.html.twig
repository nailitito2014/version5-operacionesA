<form method="post" action="{{ path('rectificacion_desglose_manifiesto_delete', {'id': rectificacion_desglose_manifiesto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rectificacion_desglose_manifiesto.id) }}">
    <button class="btn">Delete</button>
</form>
